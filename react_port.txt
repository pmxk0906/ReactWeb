-> create project react
    --> npx create-react-app my-app
    --> cd my-app
    --> npm start

/// CSS in file index.hrml ///
-> boxicons (https://boxicons.com/usage)
    --> import css ของ boxicons
-> unicons (https://iconscout.com/unicons)
    --> import css ของ unicons
-> fonts (https://fonts.google.com/)
    --> fonts poppins Regular 400, Meduim 500, SemiBold 600

/// CSS in file App.css ///
กำหนดค่า Root ของ สี ขนาดตัวอักษร สีตัวอักษร 

// ทำวงกลมเคลื่อนไหว อยู่ใน file home.css บรรทัดที่ 67-82//


// use svg flie = send, scroll //
// ใช้บ่อย //
import React from "react";

const ScrollDown = () => {
    return (
         <div></div>
    )
}

export default ScrollDown